define(function() {
    var Control = require( 'saber-control' );

    describe( 'TODO', function() {
            it( 'todo', function() {
                expect( true ).toBe( true );
            });
        });

});